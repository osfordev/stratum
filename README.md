# Stratum (Shell)

It is an orphan branch of shell implementation of Stratum Tool

## Install

```
sudo wget -O /usr/sbin/stratum https://raw.githubusercontent.com/osfordev/stratum/refs/heads/shell/stratum
sudo chmod +x /usr/sbin/stratum
```
